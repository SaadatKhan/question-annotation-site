{
  "name": "question-annotation-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev:api": "wrangler dev",
    "deploy:api": "wrangler deploy",
    "generate-secret": "node worker/scripts/generate-secret.mjs",
    "setup-users": "node worker/scripts/setup-users.mjs",
    "setup-users:auto": "node worker/scripts/setup-users.mjs --generate",
    "test:live": "node worker/scripts/smoke-test.mjs",
    "hash-password": "node worker/scripts/hash-password.mjs",
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
